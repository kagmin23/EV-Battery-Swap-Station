import { Stack } from 'expo-router';

export default function DriverLayout() {
    return (
        <Stack>
            <Stack.Screen
                name="component/MapComponent"
                options={{
                    title: 'Map',
                    headerShown: false
                }}
            />
            {/* Add more driver-related screens here as they are created */}
        </Stack>
    );
}