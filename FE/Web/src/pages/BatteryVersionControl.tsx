import PageTemplate from '../components/common/PageTemplate';

const BatteryVersionControl = () => {
  return (
    <PageTemplate 
      title="Kiểm soát phiên bản pin" 
      description="Trang kiểm soát phiên bản pin."
    />
  );
};

export default BatteryVersionControl;

